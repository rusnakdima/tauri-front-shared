// Re-export the canonical ComponentRegistryService which now handles both
// built-in components and schema-based components.
// The canonical service is in core/lib/component-registry.service.ts.
export { ComponentRegistryService } from "../component-registry.service";
